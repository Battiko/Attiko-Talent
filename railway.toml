[build]
builder = "RAILPACK"
buildCommand = "pnpm --filter @attiko/api build"

[deploy]
startCommand = "node apps/api/dist/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
